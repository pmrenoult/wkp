<nav class="navbar navbar-default navbar-static-top" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "wrkplst", root_path, class:"navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
      <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Browse Events", events_path %></li>

          <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="glyphicon glyphicon-user"></span> <%= current_user.name %></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Profile", edit_user_registration_path %></li>
              <li><%= link_to "Disconnect", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
          <li>
            <%= link_to new_event_path do %>
              <span class="glyphicon glyphicon-plus"></span> New event
            <% end %>
          </li>
          <% else %>
            <li><%= link_to "Sign in", new_user_session_path %></li>
          <% end %>
    </div><!-- /.navbar-collapse -->
  </div>
</nav>